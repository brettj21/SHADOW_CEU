# These files are included by PHP; nothing here is meant to be fetched over HTTP.
# Blocking direct access keeps class internals and any bootstrap output off the web.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
